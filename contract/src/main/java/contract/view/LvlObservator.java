package contract.view;

public interface LvlObservator {
	/**
	 * It launches the query depending on the selected lvl in the combo box
	 * @param level
	 */
	public void onLevelSelected(int level);
}
