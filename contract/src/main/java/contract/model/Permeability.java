package contract.model;

/**
 * 
 * This is the permeability.
 * An element can either be BLOCKING, in which case he would block the player, or
 * PERMEABLE which would allow the player to move through this element
 * @author Jerome
 *
 */
public enum Permeability{
	BLOCKING,
	PERMEABLE
}